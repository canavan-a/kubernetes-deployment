apiVersion: apps/v1
kind: Deployment
metadata:
  name: vitesweeper
  namespace: acanavan-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vitesweeper # app name
  template:
    metadata:
      labels:
        app: vitesweeper # app name
    spec:
      containers:
        - name: vitesweeper # container name
          image: 766216391680.dkr.ecr.us-east-2.amazonaws.com/vitesweeper:latest
          imagePullPolicy: Always

          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
      imagePullSecrets:
        - name: ecr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: vitesweeper-service
  namespace: acanavan-namespace
spec:
  selector:
    app: vitesweeper
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
